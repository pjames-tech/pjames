---
// BookingCTA.astro - High-pressure conversion section
// PERFORMANCE OPTIMIZED: Replaced blur-[100px] with radial-gradient
// Uses global .action-book class for booking trigger
---
<section class="relative w-full py-24 overflow-hidden bg-bg border-t border-[rgb(var(--border)/0.1)]">
  <!-- Grid Pattern + Orange Glow (OPTIMIZED: CSS gradient instead of blur) -->
  <div class="absolute inset-0 z-0 pointer-events-none">
    <!-- Grid Pattern -->
    <div class="absolute inset-0 opacity-20 bg-[linear-gradient(to_right,#80808012_1px,transparent_1px),linear-gradient(to_bottom,#80808012_1px,transparent_1px)] bg-[size:24px_24px]"></div>
    <!-- Glow: Radial gradient instead of blur-[100px] for performance -->
    <div class="absolute inset-0 bg-[radial-gradient(ellipse_400px_300px_at_50%_0%,rgba(249,115,22,0.15),transparent)]"></div>
  </div>

  <div class="relative z-10 max-w-4xl mx-auto text-center px-6">
    <span class="inline-block px-3 py-1 mb-6 text-xs font-medium tracking-widest text-orange-400 uppercase bg-orange-500/10 rounded-full border border-orange-500/20">
      System Ready
    </span>
    <h2 class="text-4xl md:text-5xl font-bold tracking-tight text-white mb-6">
      Stop Competing. <br class="hidden md:block" />
      <span class="text-transparent bg-clip-text bg-gradient-to-r from-orange-400 to-amber-400">
        Start Architecting Authority.
      </span>
    </h2>
    <p class="text-lg text-zinc-400 mb-10 max-w-2xl mx-auto leading-relaxed">
      You have seen the shift. You have seen the proof. The only variable left is speed.
      Let's build your dominance protocol.
    </p>
    <button
      class="action-book group relative inline-flex items-center gap-3 px-8 py-4 bg-orange-500 text-black font-bold text-lg rounded-full hover:bg-orange-400 transition-all duration-300 hover:scale-105 hover:shadow-[0_0_40px_-10px_rgba(249,115,22,0.6)]"
    >
      <span>Initialize Project</span>
      <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="group-hover:translate-x-1 transition-transform">
        <line x1="5" y1="12" x2="19" y2="12"></line>
        <polyline points="12 5 19 12 12 19"></polyline>
      </svg>
    </button>
    <p class="mt-6 text-sm text-zinc-500">Limited availability for Q1 2026.</p>
  </div>
</section>
